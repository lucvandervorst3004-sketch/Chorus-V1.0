# QRSpotify MVP

Native Android MVP in Kotlin for Android Studio.

Belangrijk: voeg een officiële `gradle-wrapper.jar` toe in `gradle/wrapper/` als je dit project niet bovenop een vers Android Studio-project plaatst.
